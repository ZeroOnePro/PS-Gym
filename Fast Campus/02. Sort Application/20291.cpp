#include <bits/stdc++.h>

using namespace std;

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n;
  cin >> n;
  // . 앞에 문자열 날리고 이후 확장자만 저장해서 정렬해서 몇 개 있는지 세기
  // 확실히 hash쓰면 매우 간편하긴 하겠다
  vector<string> file;
  for (int i = 0; i < n; ++i) {
    string file_name;
    cin >> file_name;
  }
  return 0;
}